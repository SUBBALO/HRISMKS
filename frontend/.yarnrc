# react-router@7, serialize-javascript@7, @peculiar/x509 mendeklarasikan
# engines.node ">=20" (hanya advisory — tetap berjalan di Node 18).
# Setelan ini membuat `yarn install` berjalan normal di Node 18 TANPA perlu
# menambahkan flag --ignore-engines secara manual.
--ignore-engines true
